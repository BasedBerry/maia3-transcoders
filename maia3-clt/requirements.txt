torch>=2.1
python-chess>=1.11
numpy
huggingface-hub>=0.23
zstandard          # only needed to read .pgn.zst lichess dumps directly

# Also requires the Maia-3 inference package, which provides the base chess model,
# tokenizer, and checkpoint loader used by this code:
#   pip install "maia3 @ git+https://github.com/CSSLab/maia3"
